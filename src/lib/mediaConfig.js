// ============================================================
// mediaConfig.js — הקבועים של המדיה, במקום אחד.
//
// הקובץ הזה נטען גם מ-svelte.config.js (Node רגיל, לפני שקיימים כינויי
// ‎$lib/‎ ו-‎$app/‎), ולכן הוא חייב להישאר בלי שום import. כל לוגיקה שצריכה
// את הכינויים האלה יושבת ב-$lib/img.js.
// ============================================================

/** הדומיין שממנו מוגשת המדיה של Strapi (תמונות קטגוריות, לוגואים, באנרים) */
export const MEDIA_HOST = 'api.gofreeil.com';

/** בסיס לכתובות יחסיות שחוזרות מ-Strapi (‎/uploads/...‎) */
export const MEDIA_BASE = `https://${MEDIA_HOST}`;

/** הרוחבים שממטב התמונות של Vercel מורשה להחזיר. חייב להיות זהה ל-images.sizes
 *  ב-svelte.config.js — רוחב שאינו ברשימה מוחזר כשגיאה 400 ולא כתמונה. */
export const IMG_WIDTHS = [128, 256, 384, 640, 828, 1200, 1920];
