// ============================================================
// אריחי הבסיס של שתי המפות (מפת העסקים בדף הבית ומפת אזור העבודה
// בדף העסק) — מקור אחד לכתובת, כדי שהמפתח יתעדכן במקום אחד.
//
// למה CARTO ולא OSM: אריחי OSM הרגילים צורבים את שמות היישובים אל תוך
// התמונה, ובאזור שלנו הם מגיעים בערבית לצד העברית — אין דרך לסנן שפה
// מ-PNG מוכן. לכן בסיס בלי שום כיתוב (Voyager no-labels), והשמות נכתבים
// מעליו בעברית על ידי המפות עצמן (ראו cityLabels ב-serviceArea).
//
// למה מפתח: מספטמבר 2026 CARTO דורשת מפתח גם לאריחים החינמיים, ובלעדיו
// היא מגישה אריחים עם סימן מים "API KEY REQUIRED" צרוב בתוך התמונה.
// המפתח חינמי (carto.com/basemaps/apikey — אימייל, דומיין ותיאור קצר,
// בלי חשבון) ומכסתו 5 מיליון אריחים בחודש. הוא ציבורי מטבעו — נשלח
// מהדפדפן עם כל אריח — ולכן יושב במשתנה PUBLIC_ ולא בסוד.
// ============================================================

import * as publicVars from '$env/static/public';

const CARTO_KEY = publicVars['PUBLIC_CARTO_API_KEY'] || '';

/** כתובת האריחים בתבנית של Leaflet. בלי מפתח — הכתובת הישנה כמות שהיא,
    כדי שהמפה לא תישבר בסביבת פיתוח שאין בה .env מלא. */
export const BASEMAP_URL =
	'https://{s}.basemaps.cartocdn.com/rastertiles/voyager_nolabels/{z}/{x}/{y}{r}.png' +
	(CARTO_KEY ? `?key=${encodeURIComponent(CARTO_KEY)}` : '');

export const BASEMAP_OPTIONS = { subdomains: 'abcd', maxZoom: 20 };
